program sample14;

var
com : char;
    x , y : integer;
    endflag : boolean;
    
begin
writeln ( STRING );
    x := 0;
    endflag := false;
    while not endflag do
begin
writeln ( STRING );
    readln ( com , y );
    if ( com = STRING ) or ( com = STRING ) then
begin
x := y;
    
endelse if com = STRING then
begin
x := x + y;
    
endelse if com = STRING then
begin
x := x - y;
    
endelse if com = STRING then
begin
x := x * y;
    
endelse if com = STRING then
begin
x := x div y;
    
endelse if ( com = STRING ) or ( com = STRING ) then
begin
endflag := true
endelse
begin
writeln;
    writeln ( STRING );
    writeln ( STRING );
    writeln ( STRING );
    writeln ( STRING );
    writeln ( STRING );
    writeln ( STRING );
    writeln ( STRING );
    writeln
end;
if endflag then writeln ( STRING , x ) else writeln ( STRING , x )
end
end.
