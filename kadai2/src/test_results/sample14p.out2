program sample14;

var
x , y : integer;
    procedure sum;
    
begin
x := x + y
end;
procedure sub;

begin
x := x - y
end;
procedure mult;

begin
x := x * y
end;
procedure divide;

begin
x := x div y
end;

var
com : char;
    endflag : boolean;
    
begin
writeln ( STRING );
    x := 0;
    endflag := false;
    while not endflag do
begin
writeln ( STRING );
    readln ( com , y );
    if ( com = STRING ) or ( com = STRING ) then x := y else if com = STRING then call sum else if com = STRING then call sub else if com = STRING then call mult else if com = STRING then call divide else if ( com = STRING ) or ( com = STRING ) then endflag := true else
begin
writeln;
    writeln ( STRING );
    writeln ( STRING );
    writeln ( STRING );
    writeln ( STRING );
    writeln ( STRING );
    writeln ( STRING );
    writeln ( STRING );
    writeln
end;
if endflag then writeln ( STRING , x ) else writeln ( STRING , x )
end
end.
